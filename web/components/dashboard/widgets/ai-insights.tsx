"use client";

import { NoDataWidget } from "./no-data-widget";

export function AIInsightsWidget() {
  return (
    <NoDataWidget
      title="AI Insights"
      description="Insight cards require validated historical benchmark signals."
    />
  );
}
