"use client";

import { NoDataWidget } from "./no-data-widget";

export function TrendAnalyticsWidget() {
  return (
    <NoDataWidget
      title="Trend Analytics"
      description="Trend analytics stay hidden until real historical benchmark snapshots are available."
    />
  );
}
