"use client";

import { NoDataWidget } from "./no-data-widget";

export function RoleComparisonWidget() {
  return (
    <NoDataWidget
      title="Role Comparison"
      description="Cross-role comparison requires additional benchmark coverage in this workspace."
    />
  );
}
