"use client";

import { NoDataWidget } from "./no-data-widget";

export function ExperienceMatrixWidget() {
  return (
    <NoDataWidget
      title="Experience Matrix"
      description="Level progression views are hidden until real pooled benchmark history is available."
    />
  );
}
